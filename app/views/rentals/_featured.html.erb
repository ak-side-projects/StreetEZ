<h3>Featured Listings</h3>

<ul class="featured">
  <li class="featured-1">
    <a href="<%= rental_url(rental_ex1.id) %>">
			<% if rental_ex1.photos.first %>
	      <img src="<%= rental_ex1.photos.first.file.url(:med) %>" alt="rental_listing">
			<% else %>
				<img src="#" alt="rental_listing">
			<% end %>
    </a>

    <p class="featured-1"><%= rental_ex1.num_bedrooms %> Beds</p>
    <p class="featured-1">$<%= rental_ex1.monthly_rent %> per month</p>
  </li>

  <li class="featured-2">
    <a href="<%= rental_url(rental_ex2.id) %>">
			<% if rental_ex2.photos.first %>
	      <img src="<%= rental_ex2.photos.first.file.url(:med) %>" alt="rental_listing">
			<% else %>
				<img src="#" alt="rental_listing">
			<% end %>
    </a>

    <p class="featured-2"><%= rental_ex2.num_bedrooms %> Beds</p>
    <p class="featured-1">$<%= rental_ex2.monthly_rent %> per month</p>
  </li>

  <li class="featured-3">
    <a href="<%= rental_url(rental_ex3.id) %>">
			<% if rental_ex3.photos.first %>
	      <img src="<%= rental_ex3.photos.first.file.url(:med) %>" alt="rental_listing">
			<% else %>
				<img src="#" alt="rental_listing">
			<% end %>
    </a>

    <p class="featured-3"><%= rental_ex3.num_bedrooms %> Beds</p>
    <p class="featured-1">$<%= rental_ex3.monthly_rent %> per month</p>
  </li>
</ul>